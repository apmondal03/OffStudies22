import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Prepositions",
  description: "Master English prepositions — time, place, and movement uses, plus common adjective and noun collocations.",
};

export default function PrepositionsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
